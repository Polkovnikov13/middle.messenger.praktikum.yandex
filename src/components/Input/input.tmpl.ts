export const tmpl = `
  <input
      placeholder="{{ placeholder }}"
      name="{{ name }}"
      value="{{ value }}"
      type="{{type}}"
      class="{{class}}"
      accept="{{accept}}"
      id="{{id}}"
    />
`;
